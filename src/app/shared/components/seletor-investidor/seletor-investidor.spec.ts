// T-006 — Seletor de investidor da topbar. Teste-esqueleto de `onp-spec scaffold`:
// FALHA até ser implementado. Mantenha a tag `@spec:` ao mover o teste.

describe('seletor-investidor', () => {
  it('AC-004: O seletor lista os investidores e define o contexto @spec:AC-004', () => {
    // Dado: que existem investidores cadastrados
    // Quando: abro o seletor no topo da tela
    // Então: vejo a lista de investidores e, ao escolher um, ele passa a ser o contexto
    throw new Error('AC-004 ainda não provado — implemente este teste');
  });
});
